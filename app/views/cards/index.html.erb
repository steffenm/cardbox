<h2>Liste der Karten</h2>

<table>
  <tr>
    <th>Seite&nbsp;1</th>
    <th>Seite&nbsp;2</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cards.each do |card| %>
  <tr>
    <td><%= card.side1 %></td>
    <td><%= card.side2 %></td>
    <td><%= link_to 'zeigen', card %></td>
    <td><%= link_to 'ändern', edit_card_path(card) %></td>
    <td><%= link_to 'löschen', card, :confirm => 'Bist Du sicher?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neue Karte', new_card_path %>
