<%= form_for(@card_cardbox) do |f| %>
  <% if @card_cardbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_cardbox.errors.count, "error") %> prohibited this card_cardbox from being saved:</h2>

      <ul>
      <% @card_cardbox.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :card_id %><br />
    <%= f.text_field :card_id %>
  </div>
  <div class="field">
    <%= f.label :cardbox_id %><br />
    <%= f.text_field :cardbox_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
